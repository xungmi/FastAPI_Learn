tupple = (1,2,3,4,5, 5)

# tupple[0] = 10 # immutable
# tupple.append(6) # immutable
# tupple.remove(2) # immutable

print(tupple)

#ordered, immutable, allows duplicates, indexed, allows different data types



